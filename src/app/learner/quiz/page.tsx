'use client';
import React, { Suspense } from 'react';
import QuizPage from './quizpage';

export default function Quiz() {
  return (
    <Suspense>
      <QuizPage />
    </Suspense>
  );
}

// pop ups

// hover on submit?
// can the card move?
